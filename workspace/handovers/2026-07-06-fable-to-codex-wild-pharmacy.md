# Handover: Fable → Codex — Wild Pharmacy catchy/sticky pass, batch 2

Read `AGENTS.md` first. Non-negotiables: work lands **committed → pushed → on `main` → verified live** at https://saraloosa.org. Before editing any file, `git diff HEAD origin/main -- <file>` to confirm you're not on a stale base. Edit **source** in `wildroots/wild-pharmacy/` only — never `saraloosa-os/public/` (gitignored, regenerated by `saraloosa-os/scripts/stage-wildpharmacy.mjs` at build).

## Context

Liezl asked for a full "catchier & stickier" pass over https://saraloosa.org/wild-pharmacy/ (the Labyrinth RPG, `wildroots/wild-pharmacy/index.html`, ~924 lines) and https://saraloosa.org/wild-pharmacy/pharmacopoeia/ (3D plant graph, `wildroots/wild-pharmacy/pharmacopoeia/graph.html` — note line 161 is a 298KB inlined data blob; skip it when reading).

**Batch 1 (already landed by Fable — don't redo):** pharmacopoeia guided first-visit reveal (auto-fly to Kanna, `wp_intro` localStorage flag), always-visible "what can I take for…" query chips under search (`#quickChips`), Hexi-voiced meter gate copy, frosted locked plant nodes (`window.__METER__.locked`), mobile touch hint, labyrinth-hero badges on plant cards (`LAB_HEROES` set), lose-screen codex-progress framing in index.html.

## Your tasks (batch 2)

### 1. Boot-screen hook (index.html)
- The boot screen subtitle is lore-heavy. Add a short punch line above/replacing part of it: **"13 real healing plants. 13 hours. One wrong reach wakes the minotaur."** Keep the existing longer paragraph below it in smaller/muted type. Real plants first — that's the differentiator.
- Ambient tease: loop one muted Cloudinary clip (pattern: `CLIP_BASE+"NN.mp4"` — see `setBg()` usage around the win/lose functions, `CLIP_BASE` var near top of script) at low opacity behind the boot screen so the page feels alive on arrival. Respect `prefers-reduced-motion` (skip the clip) and don't autoplay audio.

### 2. First-door teaser on boot screen (index.html)
- Before "enter the labyrinth", offer one sample riddle right on the boot screen: pick a random plant from `QUEST` (built from `stations.json`), show its riddle-door question (see the rung-1 riddle build code — search `riddle` / "Two doors"). Correct answer → flourish + "that's 1 of 13 — enter the labyrinth to meet the rest" (the button becomes the main CTA). Wrong → gentle nudge, no minotaur. Keep it skippable; don't touch clock/save state.

### 3. Plant-of-the-day chip (index.html)
- On the boot screen, a small chip: "today's herb: <name> — met it yet?" Deterministic by date: `QUEST[dayOfYear % QUEST.length]`. Click → `openAlmanac(q)` (existing function). Style like existing `.btn`s but smaller.

### 4. Mid-run share moment (index.html)
- After the **3rd** tincture is collected (see where tinctures are awarded / `gotCount()`), show a one-time toast/inline chip: "half a cabinet — share your run" reusing `drawShareCard`/`wireShare` machinery (currently only wired on the end screens). One per run, dismissible, never blocks play.

### 5. Pharmacopoeia share image (graph.html)
- The `saveBtn` handler already composites bg + WebGL canvas. Add a share-card variant: when a plant is selected, overlay onto the saved PNG: plant pic (`pics/<key>.webp`, check `PICS` set), plant name + latin, and "saraloosa.org/wild-pharmacy" wordmark, styled like `drawShareCard` in index.html (gold/green on ink). Offer it from the existing share button as a second action ("save a card").

### 6. Meta/OG copy + small tech wins
- **index.html meta:** add "Every plant is real. Every caution is real. Only the minotaur is imaginary." into og:description/twitter:description (keep length sane).
- **Maze-walk timeout (index.html):** the corridor-walk transition loads gallery images with no error path (search `maze-ways` / the walk function). Add a ~2s timeout per image: on fail/timeout, skip to next step instead of stalling blank.
- **Loading vignette (graph.html):** `#stat` says "loading the living library…" — add a tiny CSS pulse/sprout animation on it until the first `onEngineStop` fires.
- **Umami funnel events:** `track()` helpers exist in both files. Add events: `boot-teaser-answer` (right/wrong), `plant-of-day-open`, `midrun-share`, `share-card-save`. (Gate events already exist: `meter-email-gate`, `meter-buy-gate`, `meter-email-unlock`, `meter-purchase-unlock`, `intro-reveal`, `quick-chip`.)

## Verification
1. `node -e "const h=require('fs').readFileSync('<file>','utf8');[...h.matchAll(/<script>([\s\S]*?)<\/script>/g)].forEach((m,i)=>{try{new Function(m[1]);console.log(i,'OK')}catch(e){console.log(i,'FAIL',e.message)}})"` on both files.
2. `cd saraloosa-os && npm run build` must pass (stage script runs in prebuild).
3. Open the staged pages locally; click through boot → teaser → game start; pharmacopoeia load → chips → node click.
4. Commit with clear message, push `origin HEAD:main`. **Pushing does NOT deploy** — no git CI. Deploy manually from the MAIN checkout (fast-forward it to origin/main first): `cd saraloosa-os && npx netlify-cli deploy --prod --site e6f1eae9-27d8-4937-b73c-6f23cd2d220a --message "<what>"` (auth already stored). Then `curl -s https://saraloosa.org/wild-pharmacy/ | grep "<your new string>"` to verify.

## Don'ts
- Don't commit anything in `saraloosa-os/public/`, `*.bak-*/`, or Liezl's Gumroad WIP files.
- Don't flip `METER.on` — it ships dark deliberately.
- Don't claim all 13 plants are SA-native (they aren't — see memory/HANDOVER).
- Keep the compost-punk palette and lowercase-mono voice; new copy should read like the existing lines.
